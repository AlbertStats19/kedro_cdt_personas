### " parameters model_selection
use_key: 
    want: False
    # if want is true
    llave: ['Run10,xgboost_9'] # Run4,xgboost_3
    # if want is false
    estrategia: ['best_model_name'] #best_model_name	best_unbiased_model	best_fitting_model
    
Ensamble: 
    want: True
    # las posiciones deben estar de menor a mayor es decir Run1 va primero que Run3
    Algoritmos: ['Run1','Run6','Run7'] # deben ser las mismas llaves del vector 'names' en los parametros de models.
    weights: [2,1,2]
    scorer: 'backtest_scorer' # si no se coloca 'backtest_scorer','f1_n_top' tomara la de models

random_state: 42
